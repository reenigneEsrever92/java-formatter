class F {
    void m() {
        String result = builder
                .setName(name)
                .setAge(age)
                .setCity(city)
                .setZip(zip)
                .build();
    }
}
