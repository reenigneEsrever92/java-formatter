class A {
    void m() {
        int someLongVariable = alpha() + beta() + gamma() + delta() + epsilon();
        boolean condition = firstLongCondition() && secondLongCondition() && thirdLongCondition();
    }
}
