class Generic<T extends Number & Serializable, U> {
    <V extends Runnable, W> void apply(V first, W second) {
        java.util.List<Map<String, Integer>> items = new java.util.ArrayList<>();
        runGeneric(
            alpha(),
            beta(),
            gamma());
    }
}
