class F {
    void m() {
        builder
                .setName(name)
                .setAge(age)
                .setCity(city)
                .setZip(zip)
                .build()
        ;
    }
}
