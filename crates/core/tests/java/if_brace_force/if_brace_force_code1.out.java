class A {
    void m() {
        if (a)
            one();
        if (b) {
            if (c)
                three();
        }
        if (d) {
            four();
        } else
            five();
    }
}
